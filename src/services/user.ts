export const fetchUser = () => Promise.resolve({ a: '1' });

export const logout = () => Promise.resolve({ a: '1' });

export const changeAccount = (): Promise<{}> => Promise.resolve({ permission_id: '1' });
